export const add = (a, b) => a + b;
// Input -> Parameters

export const output = (x) => {
  if (x > 10) {
    return "greater than 10";
  } else {
    return "less than 10";
  }
};

// if() {

// } else {

// }
