# Today's Goal
Installing React environment using Create React App

Only Once
git init

git add .
git commit -m "message here"

Go to source control
Click on Publish

From second time
git add .
git commit -m "message here"
git push

